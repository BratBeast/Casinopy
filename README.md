# OvvraBet Casino Simulator 🎰
Лабораторна робота №1a (UML та Рефакторинг).

Це навчальний проект ("Лабораторна робота №2") з курсу "Парадигма об'єктно-орієнтованого програмування", написаний на **Python** з використанням бібліотеки **Tkinter**.


Додаток є симулятором казино, який дозволяє запускати велику кількість ітерацій трьох різних азартних ігор для збору статистики та аналізу їхніх моделей випадковості.

## 🛠️ Технічний стек

* **Мова:** Python 3
* **GUI:** `Tkinter` (та `ttk`)
* **Дизайн:** `ttkthemes` (для покращення візуального стилю)
* **Графіки:** `matplotlib` (для візуалізації результатів)
* **Багатопоточність:** Модуль `threading` (для асинхронного GUI)

## 🚀 Запуск

Для запуску проекту не потрібна компіляція.

1. Клонуйте репозиторій.

2. (Рекомендовано) Створіть та активуйте віртуальне середовище:
     ```bash
   python -m venv venv
    ```

Windows:
.\venv\Scripts\activate

macOS/Linux:
source venv/bin/activate


3. Встановіть необхідні залежності:
    ```bash
    pip install ttkthemes matplotlib
    ```

4. Запустіть головний файл програми:
    ```bash
    python main.py
    ```
## 🎯 Відповідність формальним вимогам

Цей проект був доопрацьований, щоб повністю відповідати загальним вимогам лабораторної роботи:

* **Не менше 4 вікон:**
1. `MainWindow` (головне вікно симулятора).
2. `AboutDialog` (вікно "Про програму", викликається з меню).
3. `SettingsDialog` (вікно "Налаштування" для зміни теми).
4. `ChartWindow` (вікно "Графік" для візуалізації результатів).

* **Не менше 20 елементів керування:**
Проект містить понад 20 елементів, включаючи: `tk.Menu` (x2), `ttk.LabelFrame` (x2), `ttk.Radiobutton` (x6), `ttk.Label`, `ttk.Spinbox`, `ttk.Button` (x6), `scrolledtext.ScrolledText`, `FigureCanvasTkAgg` (контейнер для графіка) та інші елементи меню (`add_command`).

* **Не менше 10 обробників подій:**
Проект містить понад 10 обробників (`command`), включаючи: `start_simulation`, `show_about_dialog`, `show_settings_dialog`, `apply_settings`, `show_chart_window`, `root.destroy`, а також `lambda`-функції та `command=window.destroy` на кнопках "OK", "Скасувати", "Закрити" у діалогових вікнах.

## 🧠 Демонстрація концепцій ООП

Проект використовує ключові принципи ООП:

* **Абстракція:**
Абстрактний базовий клас `IGame` (у `game_logic.py`) визначає загальний "контракт" (інтерфейс) для всіх ігор. Він вимагає від кожного класу-спадкоємця реалізувати методи `play_once()` та `get_game_name()`.

* **Інкапсуляція:**
Кожен клас гри (`DiceGame`, `SlotsGame`, `PistolRouletteGame`) приховує свою власну складну логіку. Наприклад, `SlotsGame` інкапсулюв свої правила, `SYMBOLS` та `WEIGHTS`. Головний додаток (`main.py`) не знає про ці деталі.

* **Поліморфізм:**
Головний клас `CasinoApp` працює з об'єктом типу `IGame`. Йому неважливо, яка *саме* гра зараз запущена — `DiceGame` чи `SlotsGame`. Він гарантовано знає, що у цього об'єкта є метод `play_once()`, і просто викликає його.

* **Чітке розділення відповідальності:**
Проект розділений на два файли:
* `game_logic.py`: "Мозок" (backend). Містить *лише* логіку ігор.
* `main.py`: "Обличчя" (GUI). Містить *лише* код інтерфейсу та керування потоками.

## 📋 Додаткові коментарі

* **Завдання:** Виконання "Програми 4 – Демонстрація випадкових подій", реалізованої як симулятор казино (`OvvraBet Simulator`).
* **Ключова функція:** Симуляція винесена у фоновий потік (`threading`), щоб уникнути "зависання" GUI під час розрахунків на великій кількості ітерацій.Ґ

## 📈 Звіт про рефакторинг та аналіз (Lab 1a)

Згідно з вимогами лабораторної роботи, проект пройшов етап глибокого рефакторингу для покращення архітектури та відповідності принципам SOLID.

### 📊 Порівняння метрик коду
Ми порівняли початкову версію (**v0.1-original**) та поточну рефакторену версію.

* **Обсяг коду:** Скорочення з ~1010 до ~670 рядків (на **34%**) завдяки усуненню дублювання (DRY).
* **Розділення відповідальності:** Висока (Виділено незалежний сервіс `SimulationEngine`).
* **Час обробки (10k ітерацій):** Покращено з ~0.025 сек до **~0.012 сек**.

### 🏗️ Ключові архітектурні зміни (SOLID)

* **Single Responsibility Principle (SRP):** Логіку масових обчислень повністю винесено з класу `CasinoApp` (GUI) у новий сервісний клас `SimulationEngine`.
* **Open/Closed Principle (OCP):** Завдяки використанню інтерфейсу `IGame`, система готова до додавання нових ігор без зміни коду двигуна симуляції.
* **Liskov Substitution Principle (LSP):** Будь-яка реалізація гри (`Dice`, `Slots`) може бути використана двигуном без порушення працездатності програми.

### ⚙️ Впроваджені інженерні практики

* **Performance Tracking:** Додано замір часу виконання за допомогою `time.perf_counter()`.
* **Professional Logging:** Впроваджено модуль `logging` для відстеження стану потоків та помилок замість `print()`.
* **Robust Charting:** Реалізовано інтелектуальну обробку вхідних даних для графіків (підтримка кортежів та текстових значень).
* **Documentation:** Налаштовано **Doxygen** для автоматичної генерації технічного звіту та діаграм взаємодії класів.
